tuple TupleTest {
  timestamp: u32;
  value: string;
}

enum EnumTest as string {
    One as "one";
    Two as "two";
}

interface InterfaceTest {
  Absolute as "absolute" {
    start: u32;
    end: u32;
  }

  Relative as "relative" {
    value: u32;
  }
}

type TypeTest {
  number: EnumTest;
}
