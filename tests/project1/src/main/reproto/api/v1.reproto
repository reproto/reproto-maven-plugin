package api.v1;

tuple TupleTest {
  timestamp: u64;
  value: string;
}

enum EnumTest {
  ONE("one", 42),
  TWO("two", 42);

  serialized_as text;

  text: string;
  value: u64;
}

interface InterfaceTest {
  Absolute {
    name "absolute";

    start: u64;
    end: u64;
  }

  Relative {
    name "relative";

    value: u64;
  }
}

type TypeTest {
  number: EnumTest;
}
