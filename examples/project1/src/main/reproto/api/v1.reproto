package api.v1;

type Duration {
  value: u64;
  unit: string;
}

interface Date {
  Absolute {
    name "absolute";

    start: u64;
    end: u64;
  }

  Relative {
    name "relative";

    difference: Duration;
  }
}

type Query {
  date: Date;
}
